export interface Types {
}
export interface page {
    name: string;
    path: string;
    icon: string;
}

export interface book {
    id: number;
    title: string;
    authors: string[];
    image: string;
    webReaderLink: string;
}
